@using Snippy.App.Models
@model CommentViewModel

<h1>Delete comment</h1>

Are you sure you want to delete this comment?

@Html.DisplayFor(m => m)

@Html.ActionLink("Delete", "DeleteComment", "Comments", new { id = string.Empty }, new { @class = "btn btn-danger" })
@Html.ActionLink("Cancel", "Details", "Snippets", new { id = Model.Snippet.Id }, new { @class = "btn btn-default" })